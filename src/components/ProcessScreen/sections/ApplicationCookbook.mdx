# Crystal Ball Application Cookbook

## Repository

- Use a monorepo to keep everything colocated for easier maintainability
- Don't use Yarn, just install npm deps for each submodule, the Yarn hoisting doesn't work
  well with ESLint/Babel/etc.
